/**
 * Launch trial duration.
 *
 * Keep this as the single source of truth for both access enforcement and
 * public trial messaging. When the launch offer returns to 7 days, change
 * this value and deploy once.
 */
export const FREE_TRIAL_DAYS = 30;
